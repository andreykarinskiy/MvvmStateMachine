<UserControl x:Class="MacroRecorder.Views.RecorderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:MacroRecorder.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="300"
             d:DesignWidth="300"
             mc:Ignorable="d">
    <Grid>
        <StackPanel>
            <TextBlock Text="{Binding Status}" />
            <Button Command="{Binding Start}">Start</Button>
            <Button Command="{Binding Pause}">Pause</Button>
            <Button Command="{Binding Stop}">Stop</Button>
        </StackPanel>
    </Grid>
</UserControl>
